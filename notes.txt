notes.txt converted to an issue log.

Issues:

- Doesn't handle nested () correctly. i.e. acos(cos(2))=
  its throwing a "missing operand" message when trying to input this.

   resolution, new function added to count parentheses in order to 
   locate the proper opening "(".

- inverse functions are not working.  i.e. cannot enter acos() into 
  the infix expression.

   resolution add references to funcName to:
   - get_calculator_function_button_presses (calculator.js)
   - precedence (parser.js)
   - associativity (parser.js)

- x! not yet implemented.

- deg/rad not fully implemented.  Trig functions currently calculate in radians only.
  
  resolution, added conversion factors to each trig function in parser.js
  Added code in javascript.js to update a contral name/value pair within parser.js
  named "degrees_or_radians" with either "deg" or "rad" based on calculator interface.

- square root not fully implemented.

- pop_until_opening_parenthesis() in calculator.js is dead code.
- update_user_messsage_display() in calculator.js is dead code.